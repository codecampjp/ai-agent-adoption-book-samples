# 第13章サンプルの依存。
# オフラインのドライラン（_common.py / 13-2 / 13-3 / 13-5 / 13-7）は【追加依存なし】。
# 標準ライブラリだけで動く（Python 3.10 以上。実行確認は 3.12）。
#
# 以下は本文で紹介する評価・観測ツールを実際に使う場合の任意依存（更新が速いので版に上限）。
# オフラインのドライランには不要なのでコメントアウトしている。
#
# deepeval>=3,<4          # 13-4_deepeval_geval.py 用（採点モデルのAPIキーが要る）
# langfuse>=3,<4          # 本番でLangfuseに観測トレースを送る場合（外部SaaS/自前ホスト）
# langsmith>=0.4,<1       # 本番でLangSmithに観測トレースを送る場合（外部SaaS）
#
# promptfoo は Node.js 製。13-4_promptfoo/ は `npm install -g promptfoo` で導入する（pip不要）。

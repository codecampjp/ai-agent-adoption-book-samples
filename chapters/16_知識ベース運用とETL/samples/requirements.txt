# 第16章サンプルの依存
#
# 16-6 の再埋め込みコスト試算（オフライン）は【標準ライブラリのみ】で動くため、
# 必須の外部依存はない。
#
# 本番で実際に再埋め込みを行うときは、使う埋め込みAPIの公式SDKを追加する（例）。
# 実額・モデル名・料金は変動が速いので、使う時点の公式ドキュメントで確認すること。
#
# openai>=1.0            # OpenAI Embeddings を使う場合
# cohere>=5.0            # Cohere Embed を使う場合
# sentence-transformers  # 自己ホストの埋め込みモデルを使う場合
